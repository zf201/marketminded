package components

templ Modal(id string) {
	<dialog id={ id } class="modal">
		<div class="modal-box">
			{ children... }
		</div>
		<form method="dialog" class="modal-backdrop">
			<button>close</button>
		</form>
	</dialog>
}
